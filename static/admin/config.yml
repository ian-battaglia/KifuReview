backend:
  name: git-gateway
  branch: master

media_folder: "static/uploads"
public_folder: "/uploads"

collections:
   - name: "stories"
    label: "Stories"
    folder: "content/stories"
    slug: "{{year}}-{{slug}}"
    